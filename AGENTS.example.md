# aniGC 工作约定模板

本文件提供公开默认约定。部署时可复制为已被 Git 忽略的 `AGENTS.md`，在本地补充授权资料范围、服务接入与验收偏好。项目 ID 配置在本地 `.env` 的 `ANIGC_REFERENCE_PROJECT_ID`，不要将真实项目标识或公司资料写入公开文件。

## 执行范围

- 只有用户明确要求生成或继续生成时，才发送图片生成或生成式编辑请求。讨论、实现、测试和评论不构成生成授权。
- Codex 负责资料判断、完整输入、查看实际图片、记录问题和指导修改；Python 负责保存、次数、版本与后端调用。
- 图片任务按 [.agents/skills/image-workflow/SKILL.md](.agents/skills/image-workflow/SKILL.md) 执行；首次使用命令先读 [本地工具](docs/local-tools.md) 与 [后端说明](docs/generation-backends.md)。

## 资料与监修

- 只使用本地约定允许的资料项目，核实每份资料的归属、角色和版本；不凭模型记忆补足关键设定。
- 搜索结果先用于定位，视觉判断必须查看实际图片，文字判断必须读取原文。记录来源、版本、已确认事实、推断和未知项。
- 每张候选独立检查用户要求、角色一致性、行为性格、构图作画、明显生成瑕疵与修改回归。问题记录位置、依据、严重性、具体改法及跨轮状态。
- 无阻断问题且无关键待核验项时才登记通过；生成模型自评、文件存在或哈希一致不能代替视觉判断。

## 次数、保存与验收

- 默认每批最多四次生成/编辑请求，包括首次、已发送失败和重试。恢复与切换后端不清零；未知请求保留次数并先核实，不自动重发。
- API 生成使用 `generate --execute --evidence-ready`，内部已包含额度预留。先保存完整输入，返回后保存全部可读候选，每轮及时登记监修。
- 原图、输入、参考快照、监修和用户评论不覆盖。只有准确通过版本进入 `final_output/`，随后等待用户验收。
- 用户反馈绑定具体图片版本，保留原话，按适用范围维护本地规则。评论本身不启动新批次生成。
- 只记录必要的 Markdown、图片和恢复状态；凭据、签名链接、私有资料和真实任务不提交到公开仓库。

## 实现与验证

保持后端可替换；尚未实现的后端不能静默回退。离线测试不证明真实生成效果、参考遵循或监修质量。报告实际完成的检查和剩余限制。
