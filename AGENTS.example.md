# aniGC 工作约定模板

本文件提供公开默认约定。部署时可复制为已被 Git 忽略的 `AGENTS.md`，在本地补充授权资料范围、服务接入与验收偏好。项目 ID 配置在本地 `.env` 的 `ANIGC_REFERENCE_PROJECT_ID`，不要将真实项目标识或公司资料写入公开文件。

## 执行范围

- 只有用户明确要求生成或继续生成时，才发送图片生成或生成式编辑请求。讨论、实现、测试和评论不构成生成授权。
- Codex 负责资料判断、完整输入、查看实际图片、记录问题和指导修改；Python 负责保存、次数、版本与后端调用。
- 图片任务按 [.agents/skills/image-workflow/SKILL.md](.agents/skills/image-workflow/SKILL.md) 执行；首次使用命令先读 [本地工具](docs/local-tools.md) 与 [后端说明](docs/generation-backends.md)。

## 资料与监修

- 只使用本地约定允许的资料项目，核实每份资料的归属、角色和版本；不凭模型记忆补足关键设定。
- 搜索结果先用于定位，视觉判断必须查看实际图片，文字判断必须读取原文。记录来源、版本、已确认事实、推断和未知项。
- 生成前由 Codex 完成导演方案，明确故事瞬间、观看重点、逐人动作/视线/接触和展示风险；外观、动作、特效分别绑定证据。用户站位要求不等于镜像构图，Codex 创作选择不自动成为用户硬性约束。
- 主动从用户叙述与资料捕捉关键互动；未指定的非关键细节可以按依据自主选择并记录。镜头服务表现力，合理遮挡、半身与背侧面可以采用，身份、关键动作和互动仍须可读。
- 每张候选先看故事与整体动作，再检查角色和局部缺陷；适用的叙事／视觉质量要求与正确性同时参与通过判断。问题记录位置、依据、严重性、具体改法及跨轮状态，不仅写「AI 感」。
- 监修检查可见部分；画外／合理遮挡且非明确要求展示、不影响关键表达的细节记不适用，不为检查而强求全身或正脸。可见部分的错误和关键不可读仍需处理。
- 生成前在 C 基线固定严格保持、允许变化和表达目标，逐张绑定参考对象与禁止迁移内容；固定商品与场景人物分开处理。用户硬要求不因失败次数而降低，关键细节不可读不能临时以简化放行。
- 监修先描述实际画面，再核对风险与证据；修改明确问题、原因假设、改动、保留内容和修复判据。局部问题优先编辑合适历史底图，整体重生成须有诊断，不无依据重复抽样。
- 无阻断问题且无关键待核验项时才登记通过；生成模型自评、文件存在或哈希一致不能代替视觉判断。

## 次数、保存与验收

- 新任务／新批次默认最多六次生成/编辑请求，包括首次、已发送失败和重试。旧批次沿用保存上限，恢复与切换后端不清零；未知请求保留次数并先核实，不自动重发。默认上限变化不是新一批生成授权。
- API 生成使用 `generate --execute --evidence-ready`，内部已包含额度预留。先保存完整输入，返回后保存全部可读候选，每轮及时登记监修。
- 原图、输入、参考快照、监修和用户评论不覆盖。只有准确通过版本进入 `final_output/`，随后等待用户验收。
- 用户反馈绑定具体图片版本，保留原话，按适用范围维护本地规则。评论本身不启动新批次生成。
- 只记录必要的 Markdown、图片和恢复状态；凭据、签名链接、私有资料和真实任务不提交到公开仓库。

## 实现与验证

保持后端可替换；尚未实现的后端不能静默回退。离线测试不证明真实生成效果、参考遵循或监修质量。报告实际完成的检查和剩余限制。

## 生成任务目录命名

新生成任务目录使用 `generations/<时间戳>-<简短任务名>-<工具>/`，例如 `-gemini`、`-gpt`、`-codex-image`、`-clip-studio`。后缀标识实际制作入口，准确型号另记任务页和每轮设置；多工具任务用 `-mixed` 并逐轮记录。既有任务不为命名规则追改目录或重置额度；用户明确要求换工具重新跑的对比任务可另建目录，链接原任务并保存本次授权。

- `codex-image` 为内置 image_gen 会话路线：完整输入通过 --submission-file 冻结，check-request 后 reserve，由 Codex 调用工具，再 finish 保存全部产物。无需 API key，型号 codex-managed 表示内部型号未知；Python generate 不代发。只接入不构成真实生图授权。
